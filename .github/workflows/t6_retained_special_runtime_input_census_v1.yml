name: T6 retained special runtime input census v1
on:
  workflow_dispatch:
  push:
    paths:
      - 'tools/t6_retail_special_runtime_input_census_v1.py'
      - 'tools/t6_oat_techset_binding_manifest_v1.py'
      - 'tools/t6_dxbc_rdef_v1.py'
      - 'manifests/render/T6_RETAIL_SPECIAL_MATERIAL_FAMILY_CENSUS_V1.json'
      - 'tools/t6_oat_9dca_gcc_compat_patch_v3.py'
      - '.github/workflows/t6_retained_special_runtime_input_census_v1.yml'
permissions:
  contents: write
jobs:
  census:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - name: Restore exact pinned OAT Unlinker cache
        id: oat-cache
        uses: actions/cache/restore@v4
        with:
          path: /tmp/oat-unlinker-cache
          key: oat-unlinker-t6-9dca965366541504b71fa8cfb7ac049cb9b717e1-compat-v3-${{ hashFiles('tools/t6_oat_9dca_gcc_compat_patch_v3.py') }}-ubuntu24-x86-debug
      - name: Require exact pinned OAT cache
        shell: bash
        run: |
          set -euo pipefail
          test "${{ steps.oat-cache.outputs.cache-hit }}" = 'true'
          BIN=/tmp/oat-unlinker-cache/Unlinker
          META=/tmp/oat-unlinker-cache/source_identity.txt
          PATCH_SHA=$(sha256sum tools/t6_oat_9dca_gcc_compat_patch_v3.py | awk '{print $1}')
          test -x "$BIN"
          grep -qx 'oat_commit=9dca965366541504b71fa8cfb7ac049cb9b717e1' "$META"
          grep -qx "compat_patch_sha256=$PATCH_SHA" "$META"
          mkdir -p /tmp/game/zone/all /tmp/oat proof/render /tmp/proof
      - name: Recover exact retained worlds
        shell: bash
        run: |
          set -euo pipefail
          declare -A RAW=(
            [mp_nuketown_2020]='6c026322a713c461a03de9815bf6eef0e959fb2ab5d82299b029b775fa1ab1e0'
            [mp_raid]='bca3b74d8df5a27b82e77f39ef82cbd87a19dedf30884600b73eb563acba5888'
            [mp_hijacked]='2e5f9b87af64645e6d2e3678bca46d458890efa9a19eb8afea4cda6e8dab4037'
            [zm_prison]='ebfd53156542bc61e842087879475c7515851ddcc6d4f0ae743099590177dcb1'
            [zm_tomb]='663c2f6e275fb6a8a2844bcc0e31808abe681ce7c2a2ddc6f616ef98fc7cd0b5'
          )
          for zone in mp_nuketown_2020 mp_raid mp_hijacked zm_prison zm_tomb; do
            f="/tmp/game/zone/all/${zone}.ff"
            curl -fL --retry 4 --retry-delay 2 -o "$f" "https://r2.houseofkublai.com/bo2/zone/all/${zone}.ff"
            echo "${RAW[$zone]}  $f" | sha256sum -c -
          done
      - name: Dump exact same-world TechniqueSets
        shell: bash
        run: |
          set -euo pipefail
          BIN=/tmp/oat-unlinker-cache/Unlinker
          for zone in mp_nuketown_2020 mp_raid mp_hijacked zm_prison zm_tomb; do
            out="/tmp/oat/${zone}"
            mkdir -p "$out"
            "$BIN" --no-color --include-assets techniqueset --output-folder "$out" "/tmp/game/zone/all/${zone}.ff" 2>&1 | tee "/tmp/proof/${zone}.log"
          done
      - name: Census exact special pixel-shader runtime inputs
        env:
          PYTHONPATH: tools
        shell: bash
        run: |
          set -euo pipefail
          python3 tools/t6_retail_special_runtime_input_census_v1.py             --family-census manifests/render/T6_RETAIL_SPECIAL_MATERIAL_FAMILY_CENSUS_V1.json             --root mp_nuketown_2020=/tmp/oat/mp_nuketown_2020             --root mp_raid=/tmp/oat/mp_raid             --root mp_hijacked=/tmp/oat/mp_hijacked             --root zm_prison=/tmp/oat/zm_prison             --root zm_tomb=/tmp/oat/zm_tomb             --out proof/render/T6_RETAIL_SPECIAL_RUNTIME_INPUT_CENSUS_V1.json
          python3 - <<'PY'
          import json
          d=json.load(open('proof/render/T6_RETAIL_SPECIAL_RUNTIME_INPUT_CENSUS_V1.json'))
          s=d['summary']
          assert s['specialTechniqueSetIdentityCount']==31
          assert s['parseErrorCount']==0
          assert s['pixelShaderPassOccurrenceCount']>0
          print(json.dumps(s,indent=2,sort_keys=True))
          print('CODE CONSTANTS',d['codeConstantAccessors'])
          print('CODE SAMPLERS',d['codeSamplerAccessors'])
          PY
          git diff --check
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add proof/render/T6_RETAIL_SPECIAL_RUNTIME_INPUT_CENSUS_V1.json
          if ! git diff --cached --quiet; then
            git commit -m 'Persist retained special runtime input census'
            pushed=0
            for attempt in 1 2 3 4 5; do
              git pull --rebase origin main
              if git push origin HEAD:main; then pushed=1; break; fi
              sleep "$attempt"
            done
            test "$pushed" = 1
          fi
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: T6_RETAIL_SPECIAL_RUNTIME_INPUT_CENSUS_V1
          path: |
            proof/render/T6_RETAIL_SPECIAL_RUNTIME_INPUT_CENSUS_V1.json
            /tmp/proof
          if-no-files-found: error
          compression-level: 9
