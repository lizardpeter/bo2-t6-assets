# T6 current-client DB priority semantic probe result — 2026-09-18

## Completed experiment

GitHub Actions workflow `T6 current client DB priority semantic probe v1` completed successfully:

- workflow id: `361179645`
- run id: `35321915806`
- job id: `105526088873`
- artifact id: `10536983259`
- artifact: `T6_CURRENT_CLIENT_DB_PRIORITY_SEMANTIC_PROBE_V1`
- artifact ZIP SHA-256: `46c019166815dc5a423aed4c3459c6ab38a45d8e4a3952df3e0be59f0a8cd8d3`
- proof JSON SHA-256: `c2555def3c2dfeea57cad21152db0eca2c61f405cb03d517b02a4ab00df8ecec`

The analyzed current Plutonium client remained the separately classified revision 5346 object with SHA-256 `770318175f0161aa7a1ff0f9a5530336836a99e72900d7608a63973e56004adf`.

## Result

The semantic probe found:

- ordered server-style priority ladder candidates: **0**
- `return 54` candidates: **0**
- `return 57` candidates: **0**

Together with the preceding exact-signature experiment (which also found zero exact server priority fragments), this rejects the narrow hypothesis that the current client conserves the independently proved dedicated-server priority routine either byte-for-byte or as the tested ordered compare/conditional-branch ladder.

This is a negative comparative result only. It does **not** prove that the current client lacks zone priority/override semantics, and it says nothing authoritative about the historical retail client.

## Proof boundary

Do not promote generic `0x3fffffff` mask occurrences, zone-name strings, adjacency, ordering, nearest matches, or current-client behavior to historical-retail function identity or duplicate-Technique ownership. The historical-retail 58 divergent child-Technique conflicts remain fail-closed.

The exact production texture denominator remains separately closed at 450 identity-deduplicated GfxImages with 75/450 exact DDS coverage.

## Next blocker / experiment

Stop spending runs on direct server-byte conservation. The next current-client comparison should recover data-flow anchors from executable references to actual zone-name strings (`common`, `patch`, `_mp`, `mp_nuketown_2020`), enumerate direct call targets and callers around those references, and only then test whether any candidate code consumes stored zone flags/masks and participates in duplicate XAsset linking. This remains comparative narrowing; retail winners require genuine historical-retail executable/runtime evidence.
